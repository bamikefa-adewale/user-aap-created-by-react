import React from "react";
import { Alert } from "@material-tailwind/react";

const Status = ({ text }) => {
  return (
    <div className="container">
      return <Alert texe="loading...">{text}</Alert>;
    </div>
  );
};

export default Status;
